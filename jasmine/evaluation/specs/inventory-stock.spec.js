describe("Inventory Stock", function () {
  //Scenario – 1
  it("Inventory Stock should be updated on sale/issue of an item", function () {
    // Fix the broken test
    let stockInHand_item1 = 11;
    let item1 = x;        // Change this value to make the test pass
    const transaction = 'SALE';
    expect(xxxxxxxxxxx).toEqual(10);
  });
  //Scenario – 2
  it("Inventory Stock should be updated on issue of an item within organization", function () {
    // Fix the broken test
    let stockInHand_item1 = 11;
    let item1 = x;      // Change this value to make the test pass
    const transaction = 'ISSUE';
    expect(xxxxxxxxxxx).toEqual(10);
  });
  //Scenario – 3
  it("Inventory Stock should be updated on receiving or procuring new item", function () {
    // Fix the broken test
    let stockInHand_item1 = 11;
    let item1 = x;      // Change this value to make the test pass
    const transaction = 'PROCUREMENT';
    expect(xxxxxxxxxxx).toEqual(12);
  });
});

// ** Iteration 2
// Remove redundant code
